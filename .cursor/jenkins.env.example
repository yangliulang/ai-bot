# Jenkins credentials for deploy-jenkins skill.
# Copy to jenkins.env and fill in (jenkins.env is gitignored).
JENKINS_BASE=https://jenkins.dw2nn.com
JENKINS_USER=your-email@chainup.com
JENKINS_TOKEN=your-api-token
