---
name: pipeline-frontend-mock
description: Run frontend.mock — build UI with Mock API, do not update status.yaml.
disable-model-invocation: true
---

# pipeline-frontend-mock

Parse **功能 ID** from the user message.

1. Task: `frontend.mock` in `handoff/pipeline/tasks.yaml`
2. Follow `.cursor/rules/frontend-agent.mdc`

Execute `frontend.mock` now.
