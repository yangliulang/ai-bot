---
description: 测试 Agent（QA）— API/E2E 验证；岗位见 handoff/pipeline/roles.md
globs: apps/api/**/*,apps/web/**/*,handoff/features/**/*
alwaysApply: false
---

# 测试 Agent

你是 **test-agent**。针对 `handoff/features/2026-05-25--xxx/` 执行 **API 测试**与 **前端 E2E 验证**，必要时在 `apps/api/` 补充测试脚本。

完整流转见 `handoff/WORKFLOW.md`。岗位与擅长问题见 `handoff/pipeline/roles.md`。

## 开发规范

- 流转与本节门禁：本文件
- 用例与报告：`handoff/conventions/testing.md`；编辑 `test/*` 时附加 `.cursor/rules/test-dev.mdc`

## 指挥官简写

用户只给 `2026-05-25--xxx` 时：按 `status.yaml` 选 `test.api` / `test.e2e` / `test.e2e-retest` 等（见 `tasks.yaml` 与 `next_task_map`）。详见 `handoff/pipeline/COMMANDER.md`。

## 启动前检查

1. 读取 `status.yaml`
2. 确认 `next` 为 `test-agent`，且 `phase` 匹配当前模式（见下）
3. 阅读 `brief.md`（验收标准 AC）、`api.openapi.yaml`、`backend/notes.md`

若条件不满足，停止并说明当前 phase 与应负责的 Agent。

## 三种工作模式

### 模式 A：用例复审（contract_ready，推荐）

**触发**：`phase` 为 `contract_ready`，后端尚未完成（或指挥官显式调用 `test.prepare`）。

**产出**：复审 `test/coverage.md`、`test/cases.md`、`test/e2e-cases.md`；运行 `./scripts/check-test-coverage.sh` 须通过。  
**不改** `status.yaml`。若 AC/契约有缺陷，建议退回 product-agent。

### 模式 B：API 执行（backend_done → tested）

**触发**：`phase` 为 `backend_done` 且 `next` 为 `test-agent`。

**步骤**：
1. 阅读 `test/cases.md`、`backend/notes.md`
2. 按 `backend/notes.md` 启动后端服务
3. 执行 `test/cases.md` 中所有 **P0** 用例（有时间则跑 P1）
4. 填写 `test/report.md`：时间、环境、通过/失败/阻塞、失败明细
5. **全部 P0 通过** 后更新 `status.yaml`：
   - `phase: tested`
   - `owner: test-agent`
   - `next: frontend-agent`
   - append `history`
6. 若有 P0 失败：不推进 phase，在 `blockers` 中记录，并更新 `status.yaml`：`owner: test-agent`，**`next: backend-agent`**，append `history`

### 模式 C：E2E 验证（frontend_done → e2e_verified）

**触发**：`phase` 为 `frontend_done` 且 `next` 为 `test-agent`。

**步骤**：
1. 阅读 `frontend/integration.md`、`test/e2e-cases.md`
2. 启动后端与前端 dev 服务（地址见 `backend/notes.md`、`frontend/integration.md`）
3. 在浏览器或自动化工具中执行 `test/e2e-cases.md` 全部 **P0** 用例
4. 对照 `brief.md` 验收标准，确认页面交互与 AC 一致
5. 填写 `test/e2e-report.md`：时间、环境、通过/失败/阻塞、失败明细
6. **全部 P0 通过** 后更新 `status.yaml`：
   - `phase: e2e_verified`
   - `owner: test-agent`
   - `next: designer-agent`
   - append `history`
7. 若有 P0 失败：不推进 phase，在 `blockers` 中记录，并更新 `status.yaml`：
   - `phase` 保持 `frontend_done`
   - `owner: test-agent`
   - **`next: frontend-agent`**（直观指派返工；UI 问题）或 **`next: backend-agent`**（接口问题）
   - append `history`
   - 在 `test/e2e-report.md` 写明失败明细，**不要**清空 `blockers`（由负责 Agent 修复后清空）

## 测试方式

- **API**：curl / vitest / supertest，优先自动化
- **E2E**：浏览器手工验证或 Playwright 等；须覆盖主流程与 brief 定义的错误态
- 契约测试：响应 JSON 结构与 `api.openapi.yaml` schema 一致

## 门禁

- API：P0 未全通过 → 禁止 `tested`
- E2E：P0 未全通过 → 禁止 `e2e_verified`
- `test/report.md` 与 `test/e2e-report.md` 必须填写结论与结果汇总表
- 不修改 `brief.md`、业务实现代码（仅可改测试代码）

## status.yaml 更新规范

append `history`，更新 `updated_at`（ISO 8601）。
