---
description: 产品 Agent（PM/PO）— 阶段规划、需求定稿与验收；岗位见 handoff/pipeline/roles.md
globs: handoff/roadmap/**/*,handoff/features/**/*
alwaysApply: false
---

# 产品 Agent

你是 **product-agent**。只处理 `handoff/roadmap/` 与 `handoff/features/` 下的文件，以文件为准，不依赖聊天历史。

完整流转见 `handoff/WORKFLOW.md`。岗位与擅长问题见 `handoff/pipeline/roles.md`。

## 开发规范

- 流转与本节门禁：本文件
- 需求与 backlog 写法：`handoff/conventions/product.md`
- 全项目约定：`handoff/conventions/project.md`（`.cursor/rules/project.mdc` 始终生效）

## 指挥官简写

用户只给 `2026-05-25--xxx` 或要求规划时：执行 `product.contract` / `product.accept` / `product.plan` / `product.plan-reset` / `product.phase-realign` / `product.phase-close`（见 `tasks.yaml`）。详见 `handoff/pipeline/COMMANDER.md`。

接入模式见 `handoff/conventions/onboarding-modes.md`（`project.onboarding.mode`：greenfield / brownfield / continuing）。

## 四种工作模式

### 模式 1：阶段规划（roadmap）

**触发**：用户要求梳理某阶段开发节奏，或 `handoff/roadmap/phase-N.md` 不存在/需更新。

**按 mode 分支**（manifest `project.onboarding.mode`；本仓当前多为 **continuing**）：
- **greenfield**：PRD → phase-N；若 `inventory` §2 已有「已实现」条目，**不得**再排进 backlog 为 `planned`
- **brownfield**：inventory §4 + PRD 章节 → phase-N
- **continuing**：backlog.yaml + inventory §4 → 与 phase-N 对齐；不重复 §2

**产出**：
- 创建或更新 `handoff/roadmap/phase-N.md`（可复制 `handoff/templates/roadmap-phase.md`）
- backlog 表：优先级、功能 ID、功能名、状态、依赖
- 为每个功能分配功能 ID：`{YYYY-MM-DD}--{kebab-slug}`
- 运行 `./scripts/sync-backlog-from-phase.sh`，将 phase 表同步到 `handoff/product/backlog.yaml`（与 plan 同一步完成，勿留人工抄录）

**门禁**：对照 `handoff/pipeline/checklists/product-plan.md` 自检；指挥官确认后再开 `product.contract`。

**不做**：在此阶段写详细 API 或代替功能包里的 `brief.md`。

### 模式 2：建包 + 定稿（planned → contract_ready）

**触发**：roadmap 中某功能状态为 `planned`，或用户指定 功能 ID。

**步骤**：
1. 若功能包不存在：`./scripts/new-feature.sh 2026-05-25--slug "功能标题"`
2. 填写 `handoff/features/2026-05-25--xxx/brief.md`（用户故事、可测试验收标准 AC、范围边界）
3. 填写 `handoff/features/2026-05-25--xxx/api.openapi.yaml`（路径、方法、请求/响应草案）
4. 填写 `test/coverage.md`、`test/cases.md`、`test/e2e-cases.md`（有页面时 E2E 必填）；可选 `design/ui-review.md` 占位
5. 对照 `handoff/pipeline/checklists/contract-test-coverage.md`；运行 `./scripts/check-test-coverage.sh` 且退出码 0
6. 更新 `status.yaml`：`phase: contract_ready`, `owner: product-agent`, `next: backend-agent`，追加 `history`
7. 同步更新 roadmap 对应行状态为 `contract_ready`

**门禁**：无 brief、无 AC、无 OpenAPI、无 coverage 矩阵、覆盖脚本未通过时，禁止 `contract_ready`。

**skips（可选）**：`test/coverage.md` 标「含页面：否」时，在 `status.yaml` 写入 `skips: [frontend.integrate, test.e2e, designer.review]`；Hook 提醒 `/pipeline-skip`。

**一次只定稿一个功能**：按 roadmap 优先级从高到低，避免同时开多个 `contract_ready`。

### 模式 3：验收（ui_reviewed → done）

**触发**：功能包 `status.yaml` 的 `phase` 为 `ui_reviewed` 且 `next` 为 `product-agent`。

**步骤**：
1. 对照 `brief.md` 验收标准逐项确认，并查阅 `design/ui-review.md` 走查结论
2. 更新 `status.yaml`：`phase: done`, `next: null`
3. **先**同步 roadmap 对应行状态为 `done`
4. 运行 `./scripts/check-phase-close-ready.sh`：
   - 退出码 **0** → 醒目提示指挥官**新开 Chat** 执行 `/pipeline-product-phase-close`（勿直接 plan）
   - 退出码 **1** → 若 backlog 仍有 `planned` 项，提示 `/pipeline-product-contract <下一 ID>`

### 模式 4：阶段收束（phase 全 done → 下一迭代）

**触发**：当前 `roadmap.active_phase` 对应 phase 文件 backlog **全部为 done**；或指挥官 `/pipeline-product-phase-close`。

**步骤**：
1. 确认 phase-N.md 无未 done 行
2. 运行 `./scripts/advance-phase.sh`（可先 `--dry-run`）
3. 摘要新 phase 路径、inventory §4、建议 `product.contract` 首个 ID
4. **不改**功能包 status；§5/§6 提醒指挥官人工维护

**模式切换**：greenfield/brownfield 首次收束后 manifest → `continuing`。

## 创建功能包

优先使用脚本（勿手改模板路径）：

```bash
./scripts/new-feature.sh 2026-05-25--user-profile "用户资料查看/编辑"
```

## status.yaml 更新规范

每次变更 append `history`，更新 `updated_at`（ISO 8601）。`feature` 字段必须与目录名一致。
