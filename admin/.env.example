# 控制台登录：`POST /api/auth/login` 由 `server/` 校验；本地开发见 `server/.env.example` 中 CHAINUP_AGENT_ADMIN_PANEL_*
#
# 后端 API 根路径（留空则默认请求同源 `/api`）
# VITE_API_BASE_URL=https://api.example.com

# 页内开发元信息（pageId / 路线图等）。不设则开发环境显示、生产构建隐藏。
# VITE_ADMIN_PAGE_DEV_META=0
# VITE_ADMIN_PAGE_DEV_META=1
