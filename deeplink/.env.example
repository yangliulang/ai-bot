# 本地覆盖（可选）
# API 前缀；开发期默认 /api 走 Vite 代理（校验与「确认绑定」同一套 httpClient）
# VITE_API_BASE_URL=/api
#
# 可选：跨域直连 Agent API 的遗留变量（绑定已优先走 VITE_API_BASE_URL；未配置时与 validate 同源 `/api`）
# VITE_AGENT_API_BASE_URL=https://api.example.com
#
# Telegram Bot 打开链接（可选；未配置时 dev → yangliulang_bot，prod build → chainup_online_ai_bot）
# VITE_TELEGRAM_BOT_URL=https://t.me/yangliulang_bot
# 生产部署可在 CI 写入：VITE_TELEGRAM_BOT_URL=https://t.me/chainup_online_ai_bot
#
# 密钥校验：POST {VITE_API_BASE_URL}/v1/agent/api-binding/validate（见 agentApiBinding.ts）
# 确认绑定：POST {VITE_API_BASE_URL}/v1/me/agent/bindings/trading-api（见 tradingApiBinding.ts）
