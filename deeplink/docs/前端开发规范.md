# Deeplink · 前端协作规范（摘要）

本文是 **`admin/docs/前端开发规范.md`** 在本工程的 **摘录与补丁**。冲突时：**用户当轮指令** > **`deeplink/agent.md`** > **`admin/docs/前端开发规范.md`** > 业界惯例。

## 补丁（C 端特有）

- **视觉**：默认 **浅色** 壳（`App.vue`），便于站外 H5；具体 Token 对齐仓库根目录 **`DESIGN.md`** 与产品稿。
- **安全叙事**：外链 / 绑定页须有 **防钓鱼** 显性提示组件（见 `shared/ui/DeeplinkSafetyStrip.vue`）；不编造域名。
- **`VITE_*`**：运行时配置须 `VITE_` 前缀；示例见 `.env.example`。

## 从主规范继承（未改语义）

- 架构：**高内聚 / 单向数据流 / 契约优先**（§2）。
- 文件头三行注释（§3）。
- 技术基线：**Vue 3 Composition API、`script setup`、Vite、Pinia、Vue Router、Tailwind、k y + Zod**（§4）。
- 目录：**`src/app` · `pages` · `features` · `entities` · `shared` · `assets`**（§5）及导入边界。

全文请 directly Read：`admin/docs/前端开发规范.md`。
